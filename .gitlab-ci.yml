stages:
    - test
    - build

test:
    image: node:17-alpine
    stage: test
    tags: 
        - test
    script:
        - npm install
        - npm test

build:
    image: docker:latest
    stage: build
    tags:
        - build
    services:
        - docker:18-dind
    script:
        - docker build


